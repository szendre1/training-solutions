package week14d02;

public class temp {
//
//    private Properties prop = new Properties();
//
//
//    public void testProperties() {
//        // set key and value
//        prop.setProperty("db.url", "localhost");
//        prop.setProperty("db.user", "mkyong");
//        prop.setProperty("db.password", "password");
//
//        // save a properties file
//        try (OutputStream outputStream = new FileOutputStream("path/to/config.properties")) {
//            prop.store(outputStream, "");
//        } catch (IOException e) {
//            throw new IllegalStateException("Cannot load file", e);
//        }
//        // load a properties file
//         try (InputStream inputStream = Properties.class.getResourceAsStream("/welcome.properties")){
//        //try (InputStream inputStream = new BufferedInputStream(Files.newInputStream(Path.of("/welcome.properties")))){
//             prop.load(inputStream)
//         } catch (IOException e) {
//             throw new IllegalStateException("Cannot save file", e);
//         }
//        // get value by key
//        prop.getProperty("db.url");
//        prop.getProperty("db.user");
//        prop.getProperty("db.password");
//
//        // get all keys
//        prop.keySet();
//
//        // print everything
//        prop.forEach((k, v) -> System.out.println("Key : " + k + ", Value : " + v));
//
//
//
//

    }
